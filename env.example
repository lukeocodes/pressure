# Pressure Campaign - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# General Configuration
# =============================================================================

# Base URL for your deployment (auto-detected on Netlify)
# BASE_URL=https://yourdomain.com

# JWT secret for magic link tokens (use a long random string in production)
JWT_SECRET=dev-secret-change-in-production

# =============================================================================
# Email Configuration
# =============================================================================

# Email provider to use: console, sendgrid, mailgun, netlify-blobs
EMAIL_PROVIDER=console

# Default sender email and name
EMAIL_FROM=noreply@example.com
EMAIL_FROM_NAME=Pressure Campaign

# -----------------------------------------------------------------------------
# SendGrid Configuration (if EMAIL_PROVIDER=sendgrid)
# -----------------------------------------------------------------------------

# EMAIL_SENDGRID_API_KEY=your_sendgrid_api_key_here

# -----------------------------------------------------------------------------
# Mailgun Configuration (if EMAIL_PROVIDER=mailgun)
# -----------------------------------------------------------------------------

# EMAIL_MAILGUN_API_KEY=your_mailgun_api_key_here
# EMAIL_MAILGUN_DOMAIN=mg.yourdomain.com

# -----------------------------------------------------------------------------
# Netlify Blobs Queue Configuration (if EMAIL_PROVIDER=netlify-blobs)
# -----------------------------------------------------------------------------

# Store name for the email queue (default: email-queue)
# EMAIL_NETLIFY_BLOBS_STORE_NAME=email-queue

# API key for authenticating external consumers (recommended for production)
# EMAIL_NETLIFY_BLOBS_API_KEY=your_long_random_api_key_here

# Default batch size for fetch requests (default: 10)
# EMAIL_NETLIFY_BLOBS_DEFAULT_BATCH_SIZE=10

# =============================================================================
# API Configuration
# =============================================================================

# Optional: Use mock API server for development
# MOCK_API_URL=http://localhost:3001

# =============================================================================
# Development Notes
# =============================================================================

# For local development, you can use:
# - EMAIL_PROVIDER=console (logs emails to console)
# - MOCK_API_URL=http://localhost:3001 (uses mock Parliament API)
#
# Run with: pnpm dev:mock
#
# For production deployment on Netlify:
# - Set EMAIL_PROVIDER to sendgrid, mailgun, or netlify-blobs
# - Configure provider-specific API keys
# - Set a strong JWT_SECRET
# - BASE_URL is auto-detected

